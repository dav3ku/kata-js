3 + 2; // 5

typeof 3; // number

typeof 3 === typeof 4.32; // true

5 / 0; // undefined

3 / "bob"; // NaN

NaN === NaN; // false

typeof NaN; // number

isNaN(NaN); // true

Math.pow(2, 3); // 8

var first_name = "cindy"; // Se declara una variable y se asigna la cadena "cindy"

var hello; // se declara una variable pero no se define su valor. si lo vemos sera undefined

var y;
y === "cool"; // false

"some" + " person"; // concatenra las dos cadenas de texto el resiltado sera: "some person"

var first = "Bart";
var last = "Simpson";
first + " " + last; // concatenara las variables más el espacio: "Bart Simpson"

"cool".length; // 4

"phat" === "phat"; // true

typeof "cats"; // string

3 + "bob"; // convertira el numero en cadena para poder concatenar: "3bob"

Math.round(4.87); // redondea al siguiente entero.

6 / "2"; // convertira la cadena en numero para poder realizar la operacion.

3 * "bob"; // NaN. Ya que no puede realizar la operación convirtiendo ni en string ni en number.

var x, y;

var hobby = "programming";

var sport;
sport === undefined; // true

typeof "brogrammer"; // string
